pipeline {
 agent any
 stages {
   stage ('building the application') {
     steps { 
       echo "Welcome to Jenkins peipeline"
        sh "hostname"
        sh "uptime"
           }
         }
        }
}
